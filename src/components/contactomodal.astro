---
// ContactModal.astro
---
<div
  id="contactModal"
  class="fixed inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm transition-opacity opacity-0 pointer-events-none"
>
  <div class="bg-white rounded-3xl shadow-xl max-w-3xl w-full flex flex-col gap-8 items-center p-10 relative overflow-hidden">
    <button id="closeModalBtn" class="absolute top-4 right-4 text-2xl font-bold text-gray-400 hover:text-primary transition" aria-label="Cerrar modal">&times;</button>
    <h2 class="text-3xl md:text-4xl font-black text-primary mb-3 drop-shadow text-center">
      Contáctanos
    </h2>
    <form class="w-full flex flex-col gap-4 items-center">
      <input
        type="text"
        placeholder="Tu nombre"
        required
        class="w-full max-w-md px-5 py-3 rounded-xl border border-primary/40 focus:outline-none focus:ring-2 focus:ring-primary bg-white text-primary placeholder-gray-400"
      />
      <input
        type="email"
        placeholder="Tu email"
        required
        class="w-full max-w-md px-5 py-3 rounded-xl border border-primary/40 focus:outline-none focus:ring-2 focus:ring-primary bg-white text-primary placeholder-gray-400"
      />
      <textarea
        placeholder="¿En qué podemos ayudarte?"
        required
        class="w-full max-w-md px-5 py-3 rounded-xl border border-primary/40 focus:outline-none focus:ring-2 focus:ring-primary bg-white text-primary placeholder-gray-400 resize-none"
        rows="4"
      ></textarea>
      <button
        type="submit"
        class="bg-primary text-white font-bold rounded-xl px-8 py-3 mt-2 shadow hover:bg-primary/90 transition"
      >
        Enviar
      </button>
    </form>
  </div>
</div>

